# DLC v2 colour-engine dependencies (dlc/engine/*).
#
# The harness spine and the named-pipe contract stay dependency-free; these
# packages are only needed by the engine: patch generation, LUT building, the
# RBF correction machine, and SPD white-point computation. They are imported
# lazily — only when an `dlc.engine.*` module is loaded — so the spine tests
# and the C++ controller path run without them.
#
# Assume the system interpreter (do NOT vendor a venv into DLC). Bounded rather
# than exact pins: robust across patch releases, capped below the next major.
# Verified working set (2026-06-15): numpy 2.4.4, scipy 1.16.1, colour-science 0.4.6.
# SciPy >=1.13 is required for the RBFInterpolator per-point smoothing array used by
# DisplayErrorModel's confidence-weighted RBF anchors.

numpy>=1.26,<3
scipy>=1.13,<2
colour-science>=0.4.4,<0.5
# calibration_profile.py loads the per-display YAML profile (lazy import; the
# synthetic() path runs without it). Mirrors the pyproject `engine` extra.
PyYAML>=6,<7

# Test-only (mirrors the pyproject `test` extra): the baked-in pytest addopts pass
# `-n auto`, so pytest without xdist fails to even parse arguments; pytest-cov backs
# the audit coverage baselines. Not imported by the engine or spine.
pytest>=8,<10
pytest-xdist>=3.6,<4
pytest-cov>=5,<8
