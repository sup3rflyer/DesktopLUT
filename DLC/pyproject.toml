[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "desktoplut-calibrator"
version = "0.1.0"
description = "Automation-first profiling and calibration companion for DesktopLUT"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
dlc = "dlc.cli:main"

[tool.setuptools.packages.find]
where = ["src"]


