"""FALD (full-array local dimming) forward model — the panel-agnostic description of a
mini-LED display that a pixel-side countermeasure shader is parameterised by.

Every quantity is a *measured* per-panel parameter (see ``docs/fald-spatial-probe-2026-09-10.md``
for the first characterisation); the model itself carries no panel knowledge.
"""
